FROM node:20-alpine

WORKDIR /app

COPY package*.json ./
RUN npm install --legacy-peer-deps

COPY . .

EXPOSE 6054

# Overridden by docker-compose (ensure-node-modules + start:docker / HTTPS).
CMD ["npm", "run", "start:docker"]
