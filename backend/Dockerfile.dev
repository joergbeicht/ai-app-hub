FROM node:20-alpine

WORKDIR /app

COPY package*.json ./
RUN npm install

COPY . .

EXPOSE 6055

# --watch: Hot Reload bei Volume-Mount aus docker-compose (siehe nestjs.mdc/docker.mdc).
CMD ["npm", "run", "start:docker"]
